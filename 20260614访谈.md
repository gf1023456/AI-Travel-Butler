# 行程一下 - 2026年6月14日改动记录

## 一、已实现的改动

### 1. 前端 UI 统一与品牌重塑（上次 commit `c33512d`）

| 改动 | 文件 | 说明 |
|---|---|---|
| 7 个公共组件 | `src/components/NavBar.vue` 等 | BottomSheet、BrandButton、EmptyState、GlassCard、NavBar、Skeleton、UserAvatar |
| 旅行风格常量 | `src/constants/travelStyles.js` | 统一 4 种风格：light/deep/food/outdoor，中英文 slug 对应 |
| 行程详情页 | `src/pages/plan-detail/index.vue` | 新增广场公共方案预览页，3 按钮（点赞/收藏/复制方案） |
| 帮助与反馈 | `src/pages/settings/feedback.vue` | 新增反馈页面 |
| 行程详情页 | `src/pages/plan/plan.vue` | 加入骨架态 banner、编辑模式、保存锁（1.5s 防抖）、发布开关 |
| 探索页 | `src/pages/explore/index.vue` | 分类筛选、瀑布流、下拉刷新、回到顶部 |
| 历史页 | `src/pages/history/index.vue` | 10 条/页分页、收藏过滤 |
| 我的页 | `src/pages/mine/index.vue` | "📌 我的收藏"入口 |

### 2. 邀请去重修复

| 改动 | 文件 | 说明 |
|---|---|---|
| 数据库迁移 | `python_server/migrate_invite.sql` | users 加 `invited_by`、`invite_count`；travel_plans 加 `category`/`is_public`/`cover_url`；新建 `plan_likes`、`plan_favorites` 表 |
| 邀请去重 | `python_server/routes/quota.py` | 每个邀请人只能被一个用户使用（通过 `users.invited_by` 字段校验） |

### 3. 广场社交系统（PlanLike + PlanFavorite）

| 改动 | 文件 | 说明 |
|---|---|---|
| 点赞模型 | `python_server/database/models.py` | `PlanLike(plan_id, user_id, unique)`；`PlanFavorite(user_id, plan_id, unique)` |
| 点赞路由 | `python_server/routes/plan_likes.py` | `POST /api/plan/{id}/like` 切换点赞 |
| 广场 API | `python_server/routes/history.py` | `/history/public` 返回 `is_liked`；`/history/favorite` 改用 `plan_favorites` 表（跨用户独立） |

### 4. 方案保存去重

| 改动 | 文件 | 说明 |
|---|---|---|
| 去重逻辑 | `python_server/routes/history.py` `/save` | 5 分钟内相同 `itinerary_summary` 返回已有方案 ID（`deduped: true`） |
| 前端防抖 | `uni-app-travel/src/pages/plan/plan.vue` | `saveLock` ref 1.5s 防抖 + 三态按钮（生成中/保存中…/保存）+ 三态 toast（已存在相同方案/保存成功） |

### 5. 探索页下拉刷新（4 次尝试后最终方案）

| 改动 | 文件 | 说明 |
|---|---|---|
| 最终方案 | `uni-app-travel/src/pages/explore/index.vue` | scroll-view 自带 refresher（`:refresher-enabled` + `:refresher-triggered` + `@refresherrefresh`） |
| pages.json | `uni-app-travel/src/pages.json` | `pages/explore/index` 移除 `enablePullDownRefresh`（scroll-view 自带，不与页面级冲突） |
| 根因 | — | 页面 `height:100vh;overflow:hidden` 时，scroll-view 拦截顶部触摸事件，页面级 `onPullDownRefresh` 永远不触发。两者不能共存。 |

### 6. 回到顶部按钮

| 改动 | 文件 | 说明 |
|---|---|---|
| 历史页 | `uni-app-travel/src/pages/history/index.vue` | `@scroll` 检测 >400rpx 显示按钮，`:scroll-top` + setTimeout 触发平滑动画 |
| 探索页 | `uni-app-travel/src/pages/explore/index.vue` | `@scroll` 检测 >600rpx 显示按钮（探索卡片更大，阈值更高） |
| CSS | 两页 `.content` | `flex:1;min-height:0` + 页面根 `height:100vh;display:flex;flex-direction:column;overflow:hidden` |

---

## 二、今天新增的改动（图片压缩）

### 7. 图片压缩存储系统

| 改动 | 文件 | 说明 |
|---|---|---|
| **新增**压缩模块 | `python_server/modules/image_compressor.py` | 下载外网图 → Pillow 缩放 800px → webp 质量 75 → MD5 命名 → 存到 `static/img/` |
| 改造获取模块 | `python_server/modules/image_fetcher.py` `enrich_images` | 三步：①并发拿外网 URL ②批量下载压缩 ③替换成本地 URL |
| 挂载静态目录 | `python_server/app.py` | `app.mount("/static", StaticFiles(directory="static"))` |
| 新增目录 | `python_server/static/img/.gitkeep` | 占位文件 |
| gitignore | `python_server/.gitignore` | 忽略 `static/img/*.webp` |

**压缩参数**：800px 宽、webp 75、5 并发信号量、8s 下载超时、5s 压缩超时、MD5 缓存

### 8. 完整 URL 配置

| 改动 | 文件 | 说明 |
|---|---|---|
| 新增配置项 | `python_server/config/settings.py` | `ServerSettings.public_base_url: str = ""` |
| .env | `python_server/.env` | `PUBLIC_BASE_URL=https://tonystark-ai.ccwu.cc` |
| .env.example | `python_server/.env.example` | 同上，带注释 |
| URL 拼接 | `python_server/modules/image_compressor.py` | `_public_url()` 拼成 `https://tonystark-ai.ccwu.cc/static/img/{hash}.webp` |
| 前端配置 | `python_server/app.py` `/api/frontend-config` | 返回 `public_base_url` 字段 |

**nginx 反向代理**（用户已配）：
```nginx
location /static/ {
    proxy_pass http://127.0.0.1:8787/static/;
    expires 30d;
    add_header Cache-Control "public, max-age=2592000";
    add_header Access-Control-Allow-Origin "*";
}
```

### 9. V4 图片超时调整

| 改动 | 文件 | 说明 |
|---|---|---|
| 超时调整 | `python_server/routes/plan_v4.py:588` | `asyncio.wait_for(image_task, timeout=15.0)` → `timeout=30.0`（图片含下载+压缩，留足余量） |

---

## 三、讨论中提出的改进方向（未实现）

### 交互模式改进

| 方向 | 说明 |
|---|---|
| 骨架→用户确认→填充 | 骨架返回后加一步"这些景点对不对？想换哪个？"再填充详情 |
| 推荐理由标注 | 每个景点加"为什么推荐这个"（热门/经典/适合拍照等） |
| 方案摘要卡片 | 顶部显示：风格/天数/预算估算/适合人群/步行量 |
| 路线间距 | 景点之间显示"步行X分钟" |
| 每天主题 | D1 经典上海线、D2 文艺上海线 |
| 天气置顶 | 天气信息放在页面最顶部，不藏在景点旁边 |
| 地图总览 tab | 行程详情页加"🗺 地图总览"和"📋 时间轴"两个 tab |

### 首页重构

| 方向 | 说明 |
|---|---|
| 灵感页做首页 | 当前首页是空白地图，新用户无内容可看。灵感广场（瀑布流）做首页，地图降级为行程子页面 |
| 底部 tab 改为 | `首页(灵感) \| 我的行程 \| 我的` |
| 地图页保留 | 作为行程详情内的"查看地图"跳转目标，不再独立 |

### 产品层面

| 方向 | 说明 |
|---|---|
| 增长引擎 | 分享时加"已为X万人规划过行程"社交证明、"你的行程被Y人收藏"激励 |
| 用户偏好记忆 | 不用每次重新输入"深度游/带小孩/预算中等" |
| 广场热度衰减 | 新内容保量曝光，避免热门永远热门 |
| 付费墙 | 订阅或单次付费模式 |
| 预订闭环 | 酒店/门票 API 接入 |
| 取消生成 | 生成中加取消按钮 |
| 密钥安全 | 所有 API 密钥从代码迁移到环境变量 |
